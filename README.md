# Advent of code 2025

I'm using this repository to store my solutions for the Advent of Code 2025 programming challenges, as a means to get familiar with C.

To run:

```bash
sh run.sh <day>
```

eg `sh run.sh 1` to run day 1's solution.

To test:

```bash
sh test.sh <day>
```
